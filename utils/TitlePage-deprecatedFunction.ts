// import { Page } from '@playwright/test';

// export async function assertPageTitle(page: Page, expectedTitle: string): Promise<boolean> {
//   const actualTitle = await page.title();
//   return actualTitle === expectedTitle;
// }
